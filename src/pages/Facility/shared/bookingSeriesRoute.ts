/** Admin route for one Recurring Booking Series' detail page (registered in routes/modules/Facility/facility.tsx). */
export const bookingSeriesDetailPath = (seriesId: string): string => `/facility/booking-series/${seriesId}`;
